1. create 'setup.py' and requirements.txt' in the root.
2. create 'src/__init__.py" in the root.
3. update setup.py and requirements.txt files
4. run 'pip install -r requirements.txt' to create a packages
   # This is like running 'mvn install' to package an application
   - creates a new '-info' folder
